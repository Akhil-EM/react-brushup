import React, { Component } from 'react';
import StateComponent from './StateComponent';

class App extends Component {
  render() {
    return (
      <div>
         this is example of state component
         <StateComponent/>
      </div>
    );
  }
}

export default App;